# Dependencias de desarrollo para la base de tests del loop de agentes.
# Producción se define en requirements.txt (revisar versionado en FASE 8).
pytest>=8.0
pytest-cov>=5.0
psycopg2-binary>=2.9
sqlglot>=25.0
