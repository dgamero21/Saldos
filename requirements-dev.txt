# Dependencias de desarrollo para la base de tests del loop de agentes.
# Producci�n se define en requirements.txt (revisar versionado en FASE 8).
pytest>=8.0
pytest-cov>=5.0
psycopg2-binary>=2.9
sqlglot>=25.0
requests>=2.28
